---
import { OG} from '@innoflex-technology/og-schema'
import BlogPost from '../layouts/BlogPost.astro';


export async function getStaticPaths() {
    const projectId = "02158fba-ae81-4dc0-a0ed-dcb3328ßdb369"
    const projectKey = "63374ec2-5c01-4dea-9317-4ed368c50e50"
    const client = new OG(projectId, projectKey)
    const posts = await client.ListRecords('post');
    console.log(posts)
	return posts.map((post:any) => ({
		params: { slug: post.slug },
		props: post,
	}));
}

const post = Astro.props as any;

---

<BlogPost {...post.data}>
	<h1>{post.title}</h1>
    <div>{post.contents}</div>
</BlogPost>